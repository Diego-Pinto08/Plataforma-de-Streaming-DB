CREATE TABLE movies (
    id SERIAL PRIMARY KEY,
    title text NOT NULL,
    director text NOT NULL,
    principal_actor text NOT NULL,
    genre text,
    year date,
    duration decimal CHECK (duration > 0)
    Foreign key (genre) references Géneros(genero),
    Foreign key (director) references Directores(name_director),
    Foreign key (principal_actor) references Actores(name_actor) 
);